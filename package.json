{
  "name": "es6compiler",
  "version": "0.0.1",
  "description": "Grunt examples using grunt tasks to compile ES6 code to ES3 code with AMD handing module loading.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "",
  "author": "",
  "license": "BSD",
  "devDependencies": {
    "grunt": "~0.4.1",
    "grunt-traceur": "0.0.1",
    "grunt-es6-module-transpiler": "~0.3.0"
  }
}
